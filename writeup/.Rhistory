# Correlation between duration and acceptability
cor.test(x=filtered_d_exploratory$mean_acc, y=filtered_d$duration, method = 'pearson')
# Correlation between duration and acceptability
filtered_d_exploratory <- filtered_d %>%
filter(duration >= 120)
# Correlation between duration and acceptability
filtered_d_exploratory <- filtered_d %>%
filter(duration >= 120)
